#include <behaviors.dtsi>
#include <dt-bindings/zmk/keys.h>

/ {
    keymap {
        compatible = "zmk,keymap";

        default_layer {
            // This key will now toggle Play/Pause for any active media on your Mac
            bindings = <&kp PSCRN>;
        };
    };
};