if SHIELD_NUMPAD

config ZMK_KEYBOARD_NAME
    default "stryke"

if ZMK_BACKLIGHT

config PWM
    default y

config LED_PWM
    default y

endif # Closes "if ZMK_BACKLIGHT"

endif # Closes "if SHIELD_NUMPAD"