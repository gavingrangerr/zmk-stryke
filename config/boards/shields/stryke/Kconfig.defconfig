if SHIELD_STRYKE

config ZMK_KEYBOARD_NAME
    default "NexusPro"

if LVGL
config LV_Z_VDB_SIZE
    int
    default 64

config LV_Z_DPI
    int
    default 148

config LV_Z_BITS_PER_PIXEL
    int
    default 1

config LV_COLOR_DEPTH_1
    bool
    default y
endif # LVGL

endif # SHIELD_STRYKE