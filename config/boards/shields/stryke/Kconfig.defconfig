if SHIELD_STRYKE

config ZMK_DISPLAY
	default y

if LVGL

config LV_Z_VDB_SIZE
	int
	default 64

config LV_Z_DPI
	int
	default 148

config LV_Z_BITS_PER_PIXEL
	int
	default 1

config LV_USE_CANVAS
	bool
	default y

endif # LVGL

endif # SHIELD_STRYKE